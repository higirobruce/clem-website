.DS_Store
*.log
node_modules/
.vercel

# Secrets — never commit
.env
.env.*

# TinaCMS admin SPA (rebuilt by Vercel on each deploy)
admin/

# Note: tina/__generated__/ IS committed on purpose — Tina Cloud reads the
# compiled schema from it to configure the project and index the branch.
