{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo 'Static deploy — re-enable Tina build (npm run tina:build) once Tina Cloud has indexed the main branch. See README.'",
  "installCommand": "echo 'No install needed for the static site.'",
  "outputDirectory": "."
}
